<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Logo -->
        <ImageView
            android:id="@+id/logoImageView"
            android:layout_width="240dp"
            android:layout_height="240dp"
            android:layout_gravity="center"
            android:src="@drawable/noui_roxo"
            android:contentDescription="Logo da empresa"
            android:layout_marginBottom="16dp" />

        <!-- Título Login -->
        <TextView
            android:id="@+id/loginTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bem-vindo!"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#333"
            android:layout_gravity="center"
            android:layout_marginBottom="20dp" />

        <!-- Campo Email -->
        <EditText
            android:id="@+id/emailEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Digite seu e-mail"
            android:padding="12dp"
            android:background="@drawable/edittext_background"
            android:inputType="textEmailAddress"
            android:layout_marginBottom="12dp"
            android:contentDescription="Digite seu e-mail de usuário" />

        <!-- Campo Senha -->
        <EditText
            android:id="@+id/passwordEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Senha"
            android:padding="12dp"
            android:background="@drawable/edittext_background"
            android:inputType="textPassword"
            android:layout_marginBottom="12dp"
            android:contentDescription="Digite sua senha" />

        <!-- Botão Entrar -->
        <Button
            android:id="@+id/loginButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Entrar"
            android:textColor="#ffffff"
            android:backgroundTint="#4f49a6"
            android:padding="14dp"
            android:textSize="16sp"
            android:layout_marginBottom="12dp"
            android:contentDescription="Clique para entrar no aplicativo" />

        <!-- Botão Criar Conta -->
        <Button
            android:id="@+id/signUpButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Criar Conta"
            android:textColor="#ffffff"
            android:backgroundTint="#f87ac8"
            android:padding="14dp"
            android:textSize="16sp"
            android:layout_marginBottom="12dp"
            android:contentDescription="Clique para criar uma nova conta" />

        <!-- Link Esqueceu a senha -->
        <TextView
            android:id="@+id/forgotPasswordLink"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Esqueceu a senha?"
            android:textColor="#4f49a6"
            android:layout_gravity="end"
            android:layout_marginTop="8dp"
            android:textSize="14sp"
            android:contentDescription="Clique se você esqueceu sua senha" />

    </LinearLayout>
</ScrollView>
